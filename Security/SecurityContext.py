def securityContext:
    We can define secuirty in a container level or a pod level , If pod level all containers inherit it. If we define diff settings in container then container settings will override. 


