def NodeSelector:
    We can assign labels on Node and use the NodeSelector field for pods, Like size: Large but only one label can be used at a time, What if we need a case where we want not small nodes are eligible for pods, So medium and large nodes but no can do with NodeSelector.
