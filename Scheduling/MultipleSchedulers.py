def MultipleScheduler:
    We can define our own scheduler. But we have to configure our pod to use the scheduler. 


def SchedulingPlugins:
    We can have extension points to which a plugin can be plugged into. 

We can define scheulder profiles as well, 
